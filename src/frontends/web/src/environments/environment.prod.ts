/**
 * Production environment configuration
 * Used with: ng build --configuration=production
 */
export const environment = {
  production: true,
  envName: 'production',
  apiUrl: 'https://api.keyarc.io',
  authUrl: 'https://auth.keyarc.io'
};
